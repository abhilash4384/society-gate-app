import { Theme } from "../components";

const styles = {
  
  labelStyle: { color: Theme.themeColor },
  appStackScreenOptions: {
    headerStyle: {
      backgroundColor: Theme.themeColor,
    },
    headerTintColor: "#fff",
  },
};

export default styles;
