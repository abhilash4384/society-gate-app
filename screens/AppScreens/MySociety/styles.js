import { StyleSheet } from "react-native";
import { Theme } from "../../../components";

const styles = {
  tabBarOptionStyle: {
    labelStyle: { fontFamily: Theme.fontSecondary },
    indicatorStyle: {
      backgroundColor: Theme.themeColor,
    },
  }
};

export default styles;
